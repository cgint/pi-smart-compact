{
  "name": "pi-smart-compact",
  "version": "0.1.0",
  "description": "Prompt-only compaction mechanism that transforms overloaded Pi sessions into minimal, high-signal continuation context — better than Pi's built-in compaction.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cgint/pi-smart-compact.git"
  },
  "author": "cgint",
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "keywords": [
    "pi-package",
    "pi",
    "coding-agent",
    "extension",
    "compaction",
    "context",
    "summarization"
  ],
  "license": "MIT",
  "files": [
    "index.ts",
    "src",
    "README.md",
    "LICENSE"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.74.0",
    "@earendil-works/pi-tui": "*",
    "@sinclair/typebox": "*"
  },
  "scripts": {
    "test": "echo \"No test specified\" && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.74.0",
    "@types/node": "^22.0.0"
  }
}